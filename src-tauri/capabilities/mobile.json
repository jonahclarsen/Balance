{
  "$schema": "../gen/schemas/android-schema.json",
  "identifier": "mobile",
  "description": "Mobile-only: camera QR-code scanning for sync pairing.",
  "platforms": ["android", "iOS"],
  "windows": ["main"],
  "permissions": ["barcode-scanner:default"]
}
